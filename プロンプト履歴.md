Walica105（トゴ）システムを作りたい。

具体的に実現させたいことを列挙していくから、要件定義を書いてくれない？まだ詰め切れてない情報は都度質問してほしい。

・LINEとDiscordの個別チャット、またはグループに招待されるbotを想定する。

・WalicaのURL（例：https://walica.jp/group/01M1P9H89WEFKGG6W5WGRMYHMX ）を検知すると、botが検知、通知設定を設定する。通知間隔は任意で変えられるようにするが、現時点では、作成されてから3日目、5日目、7日目、9日目、10日目、19日目、20日目、以降はトゴによる利息が増える1日前と、利息が上がったタイミングに通知することとする。

・通知内容は、ちょっと怖いセンパイ口調でのセリフと、現時点でのサイトの中から、「清算方法」の表を抜き出してきて、現時点での金額、利息がついてしまったら10日5割の利息を支払う人に上乗せした金額を表示させる。

・構成としては
 Discord API→Cloudflare Worker（コマンド処理、D1への通知時刻書きこみ）→Cloudflare D1 SQLite DB  
Cloudflare Cron（毎分実行、期限が来たときに）→Discord API→通知
を考えている。


ごめん、追加要件は以下。仕様書を変更してくれない？
・システムの名前は、Walicaトゴシステムに決定しよう。
・そしてD1にWalicaの内容で保存する内容としては、Walicaが作成された（URLが貼られた）時間と、登録されているメンバーの人数のみ。スクレイピング結果はDBには絶対に残さない。
・登録されているメンバーの人数と、そのグループの(botや公式アカウントを除いた)人数を照らし合わせて、そのグループの人数が登録メンバー数以下だったら、そのグループ適用モード？になり、「あと〇人払ってねえなあ？」みたいなメッセージが加わる、@メンションで、「払った」等の報告を仕切るまで通知が飛ぶモードとなる。登録されているメンバーの人数より多かったら、追加メッセージはなく、一回10日5割の報告をした時点で、通知処理は自動終了となる。




要件書の前提（Cloudflare Workers + D1、Discord/LINE対応）に合わせて最小構成を作っています。

package.json を整備し、実行スクリプトを追加
dev / deploy / typecheck
d1:migrate:local / d1:migrate:remote
Cloudflare 設定を追加: wrangler.toml
TypeScript 設定を追加: tsconfig.json
Worker 雛形を追加: src/index.ts
/health エンドポイント
/webhook/discord・/webhook/line の受け口（ダミー実装）
scheduled ハンドラ枠
D1 スキーマを追加: db/schema.sql
要件書の主要テーブルとインデックスを定義
補助ファイルを追加
.gitignore
.dev.vars.example
db/migrations/.gitkeep
README.md（セットアップ手順付き）
確認結果:

npm run typecheck は成功
linter エラーなし
次にやると良い手順（あなた側で一度だけ）:

npx wrangler login
npx wrangler d1 create walica_togo_db
作成された database_id を wrangler.toml に設定
.dev.vars.example をコピーして .dev.vars を作成し、トークン類を設定
npm run d1:migrate:local
npm run dev
このまま続けて、次は FR-01 の Walica URL検知（正規化込み）から実装に入れます。